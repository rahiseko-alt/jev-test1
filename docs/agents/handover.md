# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-19 土台（#3）を実装して提出

**決めたこと**

- ORMは使わず素のSQLで書く。SQLiteとPostgreSQLの両方を既定に据えると、ORMでは
  方言ごとに表の定義を二重に持つことになるため。理由は docs/adr/0001 に記録
- 値は必ず差し込み（`?`）で渡す。SQLの文字列に値を混ぜない
- 入口の生成AIは `INTAKE_PROVIDER` で提供元を選べる（anthropic 既定 / openai）

**次にやること**

- PR #2 を確定させる（設計文書と #3 の実装が入っている）
- #4「主張を確定できる」に着手する。ここだけが生成AIを使う工程で、確定と同時に
  検索語一式を保存する
- Docker のイメージ組み立てを、Dockerが動く場で一度確認する

**未解決の問題**

- Docker のイメージ組み立てが未確認。この作業環境にDockerのデーモンが無い
- PostgreSQL を実際に立てての確認ができていない。差し込み記号の置き換えは
  単体で検査済みだが、接続してのテストは未実施
- Jevが日本語でどれだけ正しく判定できるかは、いまだ未確認（#10）

## 2026-09-19 ファクトチェッカーの設計を終え、作業指示書を発行

**決めたこと**

- 作るものは「Jevベース OSSファクトチェッカー」。生成AIは入口の聞き取りだけに使い、
  判定はJev、最終の文面はシステムの固定テンプレートで出す。MITで公開する
- 画面の土台は React Router 8（framework mode）。Remix 3 は不採用（安定版が無く、
  ガイド15章中7章が未公開、SemVer未適用、週間DL約1.6万）
- データベースはSQLiteを既定、PostgreSQLを任意。検索はTavily、本文取得は
  @mozilla/readability、論文と撤回情報はCrossref
- 判定エンジンは差し替えできる形にする。Jevは公式が「英語が主でCJKは同等でない」と
  明記しており、日本語で使えるかは未確認のため
- 設計文書は docs/design/ の01〜05。下書きとして提出済み（PR #2）

**次にやること**

- `/implement` で #3（土台の用意）から着手する。#3 → #4 → #5 → #6 → #7 → #8 → #9 の順で、
  #10 は #9 を待たずに #7 の後から並行して進めてよい
- PR #2（設計文書）を、実装を始める前に確定させる
- Jevの日本語での一致率（#10）は、結果次第で判定エンジンの差し替えが必要になる。
  低ければ企画の前提が変わるので、早めに測る

**未解決の問題**

- Jevが日本語でどれだけ正しく判定できるかが未確認。これが企画全体の前提を握っている
- 検索APIの結果そのものは規約上保存できない（Brave・Google）。自分で取得した本文だけを保存する設計で回避しているが、Tavilyの規約本文は未確認

## テンプレートから作成

**決めたこと**

- この置き場所はテンプレートから複製したもの。開発の段取り（案内役・開始と終了の儀式・引き継ぎメモ）
  だけが入っており、製品のコードはまだ1行も無い
- 何を作るかはまだ決まっていない

**次にやること**

- `/grill-with-docs` で「何を作るか」を決める。私が質問を重ねるので、答えるだけでよい
- 決まったら `/to-spec` → `/to-tickets` → `/implement` と進む
- `README.md` の冒頭をこのプロジェクトの説明に書き換える（この1件も、書き換えたら消してよい）

**未解決の問題**

- 特になし
